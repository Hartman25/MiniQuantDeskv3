[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    integration: integration-level tests (skipped unless env vars set)
norecursedirs =
    tests/_legacy
    tests/_quarantine
